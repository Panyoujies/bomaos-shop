# 开发环境配置

# 数据源配置
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/zlianweb-demo?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=GMT%2B8&rewriteBatchedStatements=true
    username: root
    password: 123youjie
    driver-class-name: com.mysql.cj.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource

# 日志配置
logging:
  level:
    cn.zlianpay: DEBUG
    com.baomidou.mybatisplus: DEBUG
